<%= link_to "Back to my entries", entries_path%>
<%= simple_form_for @entry do |f| %>
  <button type="submit" class="button">SAVE</button>
  <%= f.label :content %>
  <%= f.rich_text_area :content %>
  <%= f.submit %>
<% end%>
