<div class="container">
  <div class="row">
    <div class="entries_flex" style="margin-top: 100px;">
      <div class="entries_logo col-5">
        My Entries
      </div>

      <div class="gratefulness px-3 col">
          <%= render "daily_gratefulness"%>
      </div>
    </div>
  </div>

  <div class="d-flex justify-content-around" style="gap:390px;">
    <div class="mt-2">
      <%= link_to new_entry_path, class: "button" do%>
        <i class="fa-solid fa-pen"></i> NEW ENTRY
      <% end %>
    </div>

    <div class="d-flex justify-content-around align-items-center" >
      <%= render "calendar_search" %>
    </div>
  </div>

  <div class="row">
      <div class="entries_cards" >
          <% @entries.each do |entry| %>
          <button class="btn w-25" onclick="location.href='/entries/<%=entry.id%>/edit'" >
            <div class="entries_card col-4">
                <%= render "entries_card", entry: entry %>
            </div>
          </button>
          <% end %>
      </div>
    </div>
   </div>

  <div class="row">
    <div class="good_memory">
      <div class="px-3 col-5">
        <iframe src="https://giphy.com/embed/VbEuHLBUPQm55MyqJg"
                width="350" height="480" frameBorder="0" class="giphy-embed" allowFullScreen>
        </iframe>
      </div>

      <div >
          <h2>Good memory</h2>
        <p>"<%= @good_memory.content %>"</p>
        <p><em><%="#{@good_memory.date.strftime("%A")}, #{@good_memory.date.strftime("%B")} #{@good_memory.date.day}" %></em></p>
      </div>
    </div>
  </div>




</div>
